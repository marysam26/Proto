@model List<Proto.Areas.SystemAdmin.Models.ReviewsView>

@{
    ViewBag.Title = "Teacher-Home";
}


<div class="content-wrapper">

    @if (!Model.Any())
    {
        <div class="jumbotron">
            <h2>No Reviews made by this Reviewer. </h2>
        </div>

    }
    @if (Model.Any())
    {

        <div>
            <h2>@ViewBag.Title</h2>
            <table class="table">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Stories</th>
                        <th>Number of Reviews</th>
                        <th>&nbsp;</th>
                        <th>&nbsp;</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var student in Model)
                    {
                        <tr>
                        </tr>

                    }
                </tbody>
            </table>
        </div>


    }
</div>

